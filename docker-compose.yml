version: '3.8'

services:
  front-react:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: front-react
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro